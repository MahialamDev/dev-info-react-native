import { Text, View } from 'react-native';
import HeroPage from './(Home)/Hero';

export default function HomeScreen() {
  return (
    <View style={{flex: 1}}>
      <HeroPage />
      
    </View>
  );
}